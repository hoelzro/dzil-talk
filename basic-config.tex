\seperatorslide{Configuration}

\begin{frame}
Dist::Zilla's config file is located at the root of your distribution's directory hierarchy, and
is named \textit{dist.ini}.
\end{frame}

\inputexample[ini]{basic.ini}

\commentary{"[@Basic]" is a what's known as a \textit{plugin bundle}.  It's simply a convenient way of bundling up plugins under a name.}

\inputexample[ini]{basic-expanded.ini}
